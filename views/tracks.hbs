<div class="title">
  <h2>Title</h2>
  <h2>Listen</h2>
</div>
{{#each items}}
  <div class="all-track">
    <div class="track">
      <h2>{{name}}</h2>
      <audio controls src="{{preview_url}}">
        <a href="{{preview_url}}">Download</a>
      </audio>
    </div>
  </div>
{{/each}}